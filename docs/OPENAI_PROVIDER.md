# AI provider

AI provider integration is implemented server-side only. The browser never receives provider secrets. Structured design suggestions must be schema-validated and then validated again against the current manifest/configuration/catalog before the user can apply them.
