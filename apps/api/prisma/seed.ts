import {Prisma,PrismaClient} from '@prisma/client';
import {createClient} from '@supabase/supabase-js';
import {readFile} from 'node:fs/promises';import {resolve} from 'node:path';
const db=new PrismaClient();
async function uploadVariant(){const url=process.env.SUPABASE_URL,key=process.env.SUPABASE_SECRET_KEY,bucket=process.env.SUPABASE_STORAGE_BUCKET??'product3d';if(!url||!key){console.warn('Supabase env missing; variant catalog row will be seeded but binary upload skipped.');return;}const client=createClient(url,key,{auth:{persistSession:false,autoRefreshToken:false}});const bytes=await readFile(resolve(process.cwd(),'../../examples/variants/wide-top-variant.glb'));const{error}=await client.storage.from(bucket).upload('catalog/variants/wide-top-variant.glb',bytes,{contentType:'model/gltf-binary',upsert:true});if(error)throw error;}
async function main(){for(const material of[{id:'mat_oak_light',name:'Light Oak',category:'WOOD',propertiesJson:{baseColor:'#c69c6d',roughness:.65,metalness:0,allowColorTint:true},styleTags:['scandinavian','light']},{id:'mat_walnut',name:'Walnut',category:'WOOD',propertiesJson:{baseColor:'#6b4430',roughness:.58,metalness:0,allowColorTint:true},styleTags:['classic','warm']},{id:'mat_matte_black',name:'Matte Black Metal',category:'METAL',propertiesJson:{baseColor:'#262626',roughness:.72,metalness:.72,allowColorTint:true},styleTags:['industrial']}])await db.materialPreset.upsert({where:{id:material.id},update:material,create:material});await uploadVariant();await db.componentVariant.upsert({where:{id:'variant_top_wide'},update:{},create:{id:'variant_top_wide',groupId:'tops',role:'TOP',name:'Wide Top',assetUrl:'supabase://product3d/catalog/variants/wide-top-variant.glb',compatibilityJson:{roles:['TOP']},metadataJson:{anchorType:'BOUNDS_CENTER',dimensionPolicy:'AUTO_FIT',sourceDimensionsMm:{width:1500,height:600,depth:800}},active:true}});await db.stylePreset.upsert({where:{id:'style_scandinavian'},update:{},create:{id:'style_scandinavian',name:'Scandinavian Light',description:'Light wood surfaces with restrained dark metal accents.',styleTags:['scandinavian','light'],rulesJson:[{type:'SET_MATERIAL',target:{role:'TOP'},materialId:'mat_oak_light'},{type:'SET_MATERIAL',target:{role:'LEG'},materialId:'mat_matte_black'}] as Prisma.InputJsonValue,active:true}});await db.stylePreset.upsert({where:{id:'style_warm_walnut'},update:{},create:{id:'style_warm_walnut',name:'Warm Walnut',description:'Warm walnut finish for primary surfaces.',styleTags:['classic','warm'],rulesJson:[{type:'SET_MATERIAL',target:{role:'TOP'},materialId:'mat_walnut'}] as Prisma.InputJsonValue,active:true}});}
main().finally(()=>db.$disconnect());
