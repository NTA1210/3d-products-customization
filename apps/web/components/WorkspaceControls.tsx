'use client';import {useEffect,useState} from 'react';import {useAuthStore} from '../lib/auth-store';import {createProject,exportAndDownload,listProjects,loadProject,saveVersion,type ProjectSummary,type ProjectVersion} from '../lib/project-api';import {useEditorStore} from '../lib/store';
export default function WorkspaceControls(){const user=useAuthStore(s=>s.user),store=useEditorStore();const[projects,setProjects]=useState<ProjectSummary[]>([]),[versions,setVersions]=useState<ProjectVersion[]>([]),[busy,setBusy]=useState(false);const refresh=async()=>{if(!user){setProjects([]);return}try{setProjects(await listProjects())}catch(e){useEditorStore.setState({error:e instanceof Error?e.message:'Could not load projects.'})}};useEffect(()=>{void refresh()},[user?.id]);const create=async()=>{if(!store.assetId||!store.configuration)return;setBusy(true);try{const project=await createProject(store.assetId,store.assetName?.replace(/\.glb$/i,'')||'3D Project');await saveVersion(project.id,'Initial',store.configuration);store.setProjectId(project.id);await refresh()}catch(e){useEditorStore.setState({error:e instanceof Error?e.message:'Could not create project.'})}finally{setBusy(false)}};const save=async()=>{if(!store.projectId||!store.configuration)return;setBusy(true);try{await saveVersion(store.projectId,`Version ${new Date().toLocaleString()}`,store.configuration);await refresh()}catch(e){useEditorStore.setState({error:e instanceof Error?e.message:'Could not save version.'})}finally{setBusy(false)}};const open=async(id:string,versionId?:string)=>{if(!id)return;setBusy(true);try{const loaded=await loadProject(id,versionId);store.hydrateProject(loaded);setVersions(loaded.versions)}catch(e){useEditorStore.setState({error:e instanceof Error?e.message:'Could not load project.'})}finally{setBusy(false)}};if(!user)return <span className="muted">Sign in to save projects.</span>;return <div className="row"><select aria-label="Projects" value={store.projectId??''} onChange={e=>void open(e.target.value)}><option value="">Projects…</option>{projects.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select>{versions.length>0&&<select aria-label="Versions" defaultValue="" onChange={e=>store.projectId&&void open(store.projectId,e.target.value)}><option value="">Versions…</option>{versions.map(v=><option key={v.id} value={v.id}>{v.name}</option>)}</select>}<button disabled={busy||!store.assetId||!store.configuration||Boolean(store.projectId)} onClick={()=>void create()}>Create Project</button><button disabled={busy||!store.projectId||!store.configuration} onClick={()=>void save()}>Save Version</button><button disabled={busy||!store.projectId||!store.configuration} onClick={()=>store.projectId&&store.configuration&&void exportAndDownload(store.projectId,store.configuration).catch(e=>useEditorStore.setState({error:e instanceof Error?e.message:'Export failed.'}))}>Export GLB</button></div>}
