'use client';import {useEffect,useState} from 'react';import {getStyles,getVariants,type RuntimeVariant,type StyleRecord} from '../lib/catalog-api';import {useEditorStore} from '../lib/store';
export default function StyleVariantTools(){const{manifest,selected,configuration,setVariants,variants,dispatch,applyRules}=useEditorStore();const[styles,setStyles]=useState<StyleRecord[]>([]);const definition=manifest?.components.find(c=>c.id===selected),state=selected?configuration?.components[selected]:undefined;useEffect(()=>{void getStyles().then(setStyles).catch(e=>useEditorStore.setState({error:e instanceof Error?e.message:'Could not load styles.'}));},[]);useEffect(()=>{if(!definition)return;void getVariants(definition.variantGroupId,definition.role).then(setVariants).catch(e=>useEditorStore.setState({error:e instanceof Error?e.message:'Could not load variants.'}));},[definition?.id,definition?.variantGroupId,definition?.role,setVariants]);const list=Object.values(variants).filter(v=>(!definition?.variantGroupId||v.groupId===definition.variantGroupId)&&(!definition||v.role===definition.role));return <div className="field-group"><div className="eyebrow">Style & Variant</div><label>Style</label><select defaultValue="" onChange={e=>{const style=styles.find(s=>s.id===e.target.value);if(style)applyRules(style.rulesJson,'STYLE',`Apply style ${style.name}`,style.id);}}><option value="">Choose style…</option>{styles.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select>{definition&&state&&<><label>Variant</label><select value={state.variantId??''} disabled={!definition.variantGroupId} onChange={e=>{if(!e.target.value)dispatch({type:'RESTORE_COMPONENT',componentId:definition.id,source:'MANUAL'},'Restore original component');else dispatch({type:'REPLACE_COMPONENT',componentId:definition.id,variantId:e.target.value,source:'MANUAL'},'Replace component variant');}}><option value="">Original component</option>{list.map((v:RuntimeVariant)=><option key={v.id} value={v.id}>{v.name} · {v.dimensionPolicy}</option>)}</select></>}</div>}
